import java.util.Scanner;

public class Stock {
	public static void main(String[] args) {		
	
	Scanner input = new Scanner(System.in);
	
	//Stock symbol and name
	System.out.println("Enter the stock's name:");
	String name = input.nextLine();
	System.out.println("Enter " + name + "'s symbol:");
	String symbol = input.next();
	
	//receive previous closing price (pCP) and current price (cP)
	System.out.println("Enter " + name + "'s previous closing price:");
	float pCP = input.nextFloat();
	System.out.println("Enter " + name + "'s current price:");
	float cP = input.nextFloat();
	
	
		//calculate price change percentage
				
	
	new Stock(); {
			if (pCP > cP) {
				float negativeTPC = ((pCP - cP) * 100);
				System.out.println("The price change percentage is: -" + (negativeTPC / pCP) + "%");
			}
			else if (cP > pCP) {
				float positiveTPC = ((cP - pCP) * 100);
				System.out.println("The price change percentage is: +" + (positiveTPC / pCP) + "%");
			}
			else {
				System.out.println("There is no difference");
			}	
		}
		
		
		
	
	
	
	

	}

}

